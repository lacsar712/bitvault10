# Bitvault · 设备固件 A/B 槽与反回滚计数器

## 这不是什么

不是密码箱、不是网盘同步、不是对象存储网关。记录是 **打到设备双槽的固件镜像元数据**：槽位、单调计数器、反回滚。

## 谁在用

工控网关/机顶盒产线。设备有 A/B 根文件系统槽。新镜像的 `counter` 必须严格大于该设备已确认的计数，否则视为回滚攻击或刷旧包。

## 核心业务

1. 正文含 `slot=a|b` 与 `counter=` 非负整数。
2. 全局 setting `rollback_min`：新 counter 必须 ≥ 该值；成功后把 `rollback_min` 更新为本次 counter。
3. 标签为硬件系列 `gw-rev3` 等。
4. 附件是镜像 blob 的落地文件名（仍走路径安全拼接）。
5. 短 ID 用于镜像批次号，写入审计。

## 运行与验收

- 先写入 `counter=5` 成功，再写 `counter=3` 必须拒绝。
- 首页是产线烧录台，不是文件浏览器。
